package bonzai;

/**
 * A general interface for the various types of actions. Each turn, a unit will
 * be prompted to select a single action, which if valid will be applied to the
 * game state.
 **/
public interface Action {}
